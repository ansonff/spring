#redis配置
#Redis?据?索引（?存?使用此索引??的?据?）
spring.redis.database=0
#Redis服?器地址
spring.redis.host=127.0.0.1
#Redis服?器?接端口
spring.redis.port=6379
#Redis服?器?接密?（默??空）
spring.redis.password=
#?接超??? 毫秒（默?2000）
#?求redis服?的超???,?里注意?置成0?取默???2000
spring.redis.timeout=2000
#?接池最大?接?（使用?值表示?有限制）
#建????期望QPS/一??接的QPS,例如50000/1000=50
#一次命令??(borrow|return resource+Jedis?行命令+网?延?)的平均耗???1ms,一??接的QPS大?是1000
spring.redis.pool.max-active=50 
#?接池中的最大空??接
#建?和最大?接?一致,??做的好?是?接??不?少,?而避免了?接池伸??生的性能??。
spring.redis.pool.max-idle=50
#?接池中的最小空??接
#建??0，在??求的??下?不?建?接
spring.redis.pool.min-idle=0 
#?接池最大阻塞等待?? 毫秒（-1表示?有限制）
#建?不要?-1，?接池占?后?法?取?接??在????阻塞等待，超?后??出异常。
spring.redis.pool.max-wait=2000

